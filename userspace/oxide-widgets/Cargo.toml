[package]
name = "oxide-widgets"
version = "0.1.0"
edition = "2024"

[lib]
name = "oxide_widgets"
crate-type = ["rlib"]

[dependencies]
oxide-rt = { path = "../oxide-rt" }

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
strip = true

[profile.dev]
panic = "abort"
